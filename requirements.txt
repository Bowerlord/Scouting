# ══════════════════════════════════════════════════════════════════════════════
# KCorp Scouting Tool — Dépendances
# ══════════════════════════════════════════════════════════════════════════════
# Pour installer : pip install -r requirements.txt

# --- Data ---
pandas>=2.0
numpy>=1.24
requests>=2.31
python-dotenv>=1.0

# --- Machine Learning (Scikit-Learn) ---
# scikit-learn : le pipeline utilise le scorer natif "average_precision"
#   (compatible 1.3 → 1.6+, contrairement à make_scorer(needs_proba=True)).
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3

# --- Deep Learning (PyTorch) ---
torch>=2.0

# --- Explicabilité & Réduction dimensionnelle ---
shap>=0.42
umap-learn>=0.5

# --- Visualisation ---
matplotlib>=3.7
seaborn>=0.12
plotly>=5.15

# --- Dashboard interactif (app/) ---
streamlit>=1.30

# --- Data Sources ---
mwclient>=0.10            # Leaguepedia (MediaWiki API)

# --- Logging ---
loguru>=0.7

# --- Dev (optionnel) ---
# Outils de développement (tests, lint, format) → voir requirements-dev.txt
#   pip install -r requirements-dev.txt
