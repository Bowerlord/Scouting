# ══════════════════════════════════════════════════════════════════════════════
# ERL Scout — Dépendances
# ══════════════════════════════════════════════════════════════════════════════
# Pour installer : pip install -r requirements.txt

# --- Data ---
pandas>=2.0
numpy>=1.24
requests>=2.31

# --- Machine Learning (Scikit-Learn) ---
# scikit-learn : le pipeline utilise le scorer natif "average_precision"
#   (compatible 1.3 → 1.6+, contrairement à make_scorer(needs_proba=True)).
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3

# --- Réduction dimensionnelle (optionnelle : fallback PCA si absent) ---
umap-learn>=0.5

# --- Visualisation ---
matplotlib>=3.7
plotly>=5.15

# --- Dashboard interactif (app/) ---
streamlit>=1.30

# --- Page Agent du dashboard (app/pages/4_Agent.py) ---
# L'agent appelle l'API (httpx), la verite terrain est calculee en SQL (duckdb,
# pyyaml), et le modele parle le dialecte OpenAI (Groq).
httpx>=0.27
pyyaml>=6.0
duckdb>=1.0
openai>=1.0
# Repli local de l'agent quand l'API ne répond pas (mcp_server/backend.py) :
# il rejoue l'API en mémoire, ce qui exige fastapi. Sans lui, une API en veille
# mettait tous les outils de la page Agent en erreur.
fastapi>=0.110

# --- Logging ---
loguru>=0.7

# NB : torch, shap, seaborn, mwclient et python-dotenv ont été retirés —
# aucun module du projet ne les importe (seaborn n'est utilisé que dans les
# notebooks → requirements-dev.txt). Leaguepedia est requêté via `requests`.

# --- Dev (optionnel) ---
# Outils de développement (tests, lint, format) → voir requirements-dev.txt
#   pip install -r requirements-dev.txt
