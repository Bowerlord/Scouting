# ══════════════════════════════════════════════════════════════════════════════
# KCorp Scouting Tool — Dépendances
# ══════════════════════════════════════════════════════════════════════════════
# Pour installer : pip install -r requirements.txt

# --- Data ---
pandas>=2.0
numpy>=1.24
requests>=2.31

# --- Machine Learning (Scikit-Learn) ---
# scikit-learn : le pipeline utilise le scorer natif "average_precision"
#   (compatible 1.3 → 1.6+, contrairement à make_scorer(needs_proba=True)).
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3

# --- Réduction dimensionnelle (optionnelle : fallback PCA si absent) ---
umap-learn>=0.5

# --- Visualisation ---
matplotlib>=3.7
plotly>=5.15

# --- Dashboard interactif (app/) ---
streamlit>=1.30

# --- Logging ---
loguru>=0.7

# NB : torch, shap, seaborn, mwclient et python-dotenv ont été retirés —
# aucun module du projet ne les importe (seaborn n'est utilisé que dans les
# notebooks → requirements-dev.txt). Leaguepedia est requêté via `requests`.

# --- Dev (optionnel) ---
# Outils de développement (tests, lint, format) → voir requirements-dev.txt
#   pip install -r requirements-dev.txt
