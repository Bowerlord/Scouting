# ══════════════════════════════════════════════════════════════════════════════
# ERL Scout — Dépendances de développement
# ══════════════════════════════════════════════════════════════════════════════
# Outils de test, lint et formatage. Utilisés en local et par la CI GitHub Actions.
# Installation : pip install -r requirements-dev.txt

-r requirements.txt

# --- Tests ---
pytest>=7.0
pytest-cov>=4.0

# --- Qualité du code ---
black>=23.0
ruff>=0.1

# --- Notebooks ---
ipykernel>=6.0
jupyter>=1.0
seaborn>=0.12       # utilisé uniquement dans notebooks/01 et 02

# --- Serveur MCP ---
mcp>=2.0
pyyaml>=6.0
httpx>=0.27

# --- Agent et banc d'evaluation ---
duckdb>=1.0        # verite terrain du banc, calculee en SQL
openai>=1.0        # dialecte partage par OpenAI, Groq, DeepSeek, Mistral, GLM, OpenRouter, Ollama
anthropic>=0.40    # implementation native d'Anthropic
# Ces deux paquets ne servent qu'aux executions avec un fournisseur reel. La CI
# tourne sur la reference deterministe et n'a besoin d'aucun des deux.
